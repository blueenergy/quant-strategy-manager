# Logging Configuration Examples

# ==============================================================================
# MULTI-BACKEND SUPPORT (可同时写多个后端)
# ==============================================================================

# 同时写本地和ELK (推荐用于生产环境)
# LOG_BACKENDS=file,elk
# LOG_FILE_PATH=/var/log/trading/app.log
# LOG_FILE_MAX_BYTES=10485760
# LOG_FILE_BACKUP_COUNT=5
# LOG_ELK_HOST=elasticsearch.example.com
# LOG_ELK_PORT=9200
# LOG_ELK_INDEX=trading-logs
# LOG_ELK_USERNAME=elastic                    # ELK认证用户名（可选）
# LOG_ELK_PASSWORD=changeme                   # ELK认证密码（可选）
# LOG_ELK_USE_SSL=true                        # 使用HTTPS连接（可选，默认false）
# LOG_ELK_VERIFY_CERTS=true                   # 验证SSL证书（可选，默认true）

# 同时写本地和Loki (轻量级，推荐小规模)
# LOG_BACKENDS=file,loki
# LOG_FILE_PATH=/var/log/trading/app.log
# LOG_LOKI_HOST=loki.example.com
# LOG_LOKI_PORT=3100

# 完整监控: 本地 + ELK + Graylog
# LOG_BACKENDS=file,elk,graylog
# LOG_FILE_PATH=/var/log/trading/app.log
# LOG_ELK_HOST=elasticsearch.example.com
# LOG_ELK_PORT=9200
# LOG_GRAYLOG_HOST=graylog.example.com
# LOG_GRAYLOG_PORT=12201


# ==============================================================================
# SINGLE BACKEND EXAMPLES (单后端配置)
# ==============================================================================

# 仅本地文件 (默认，推荐起始)
# LOG_BACKENDS=file
# LOG_FILE_PATH=/home/shuyolin/trading/logs/app.log
# LOG_FILE_MAX_BYTES=10485760
# LOG_FILE_BACKUP_COUNT=5
# LOG_LEVEL=INFO

# 仅ELK
# LOG_BACKENDS=elk
# LOG_ELK_HOST=localhost
# LOG_ELK_PORT=9200
# LOG_ELK_INDEX=logs
# LOG_ELK_USERNAME=elastic                    # 生产环境通常需要
# LOG_ELK_PASSWORD=yourpassword
# LOG_ELK_USE_SSL=true                        # 生产环境推荐开启
# LOG_LEVEL=INFO

# 仅Loki
# LOG_BACKENDS=loki
# LOG_LOKI_HOST=localhost
# LOG_LOKI_PORT=3100
# LOG_LEVEL=INFO

# 仅Graylog
# LOG_BACKENDS=graylog
# LOG_GRAYLOG_HOST=localhost
# LOG_GRAYLOG_PORT=12201
# LOG_LEVEL=INFO


# ==============================================================================
# Setup Instructions by Backend
# ==============================================================================

# LOCAL FILE (simplest, recommended for starting):
# - No additional setup needed
# - Logs stored locally in specified file
# - Good for small-scale deployments

# ELK Stack (production-grade):
# docker run -d -p 9200:9200 -e "discovery.type=single-node" docker.elastic.co/elasticsearch/elasticsearch:7.14.0
# docker run -d -p 5601:5601 docker.elastic.co/kibana/kibana:7.14.0
# Then visualize logs in Kibana at http://localhost:5601

# Loki (lightweight alternative):
# docker run -d -p 3100:3100 -v /path/to/loki-config.yml:/etc/loki/local-config.yml grafana/loki:latest
# docker run -d -p 3000:3000 grafana/grafana:latest
# Then visualize in Grafana

# Graylog (full-featured):
# docker run -d -p 9000:9000 -p 1514:1514/udp graylog/graylog:4.0
# Access at http://localhost:9000 (admin/admin)
